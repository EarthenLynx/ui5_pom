<mvc:View controllerName="sap.ui.demo.basicTemplate.controller.Home" displayBlock="true"
  xmlns="sap.m"
  xmlns:l="sap.ui.layout"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns:core="sap.ui.core">
  <Table inset="false" items="{Pomodoro>/history}">
    <headerToolbar>
      <OverflowToolbar>
        <content>
          <Title text="Tasklist" level="H2"/>
          <ToolbarSpacer />
          <!--  -->
          <Button text="Synchronize" tooltip="This will try and find older session data, if you have the history option enabled" press="handleSynchronizeHistory" />
          <Button text="Clear" tooltip="This will only delete tasks from your current session. To delete history data, go to 'Settings'" press="handleDeleteHistory" />
        </content>
      </OverflowToolbar>
    </headerToolbar>
    <infoToolbar>
      <OverflowToolbar>
        <Label text="Your past pomodoros are logged here. You can permanently remove older tasks in your settings"/>
      </OverflowToolbar>
    </infoToolbar>
    <columns>
      <Column width="12em">
        <Text text="Pomodoro" />
      </Column>
      <Column minScreenWidth="Tablet" demandPopin="true">
        <Text text="Description" />
      </Column>
      <Column minScreenWidth="Desktop" demandPopin="true" hAlign="End">
        <Text text="Dauer" />
      </Column>
      <Column minScreenWidth="Desktop" demandPopin="true" hAlign="Center">
        <Text text="Typ" />
      </Column>
    </columns>
    <items>
      <ColumnListItem>
        <cells>
          <ObjectIdentifier title="{Pomodoro>title}" text="{ProductId}"/>
          <Text text="{Pomodoro>desc}" />
          <Text text="{path: 'Pomodoro>time', formatter: '.formatter.formatMsToMinutes'}  " />
          <Text text="{path: 'Pomodoro>status', formatter: '.formatter.formatStatusToString'} " />
        </cells>
      </ColumnListItem>
    </items>
  </Table>
</mvc:View>