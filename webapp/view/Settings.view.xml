<mvc:View controllerName="sap.ui.demo.basicTemplate.controller.Settings" displayBlock="true"
  xmlns="sap.m"
  xmlns:l="sap.ui.layout"
  xmlns:f="sap.ui.layout.form"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns:core="sap.ui.core">
  <f:SimpleForm title="Settings" editable="true" layout="ResponsiveGridLayout" labelSpanXL="4" labelSpanL="4" labelSpanM="4" labelSpanS="12" adjustLabelSpan="false" emptySpanXL="0" emptySpanL="0" emptySpanM="0" emptySpanS="0" columnsXL="3" columnsL="2" columnsM="2" singleContainerFullSize="false">
    <f:toolbar>
      <Toolbar id="TB1">
        <Title id="Title1" text="Address"/>
        <ToolbarSpacer />
        <Button icon="sap-icon://reset" tooltip="Restore standard settings" press="handleResetUserSettings"/>
        <Button icon="sap-icon://save" tooltip="Save your settings" press="handleSetUserSettings" />
      </Toolbar>
    </f:toolbar>
    <f:content>
      <core:Title text="Pomodoro intervals" />
      <Label text="Focus interval - {path: 'Config>/settings/pomodoro/msTotal', formatter: '.formatter.formatMsToMinutes'}" />
      <Slider step="60000" value="{Config>/settings/pomodoro/msTotal}" min="0" max="3000000" liveChange="handleSynchronizeMinFocus" class="sapUiTinyMarginBottom" />

      <Label text="Short break - {path: 'Config>/settings/shortBreak/msTotal', formatter: '.formatter.formatMsToMinutes'}" />
      <Slider step="7500" value="{Config>/settings/shortBreak/msTotal}" min="0" max="600000" class="sapUiTinyMarginBottom" />

      <Label text="Long break - {path: 'Config>/settings/longBreak/msTotal', formatter: '.formatter.formatMsToMinutes'}" />
      <Slider step="30000" value="{Config>/settings/longBreak/msTotal}" min="0" max="1500000" class="sapUiTinyMarginBottom" />

      <Label text="Min. focus - {path: 'Config>/settings/minFocus/msTotal', formatter: '.formatter.formatMsToMinutes'}" />
      <Slider step="30000" value="{Config>/settings/minFocus/msTotal}" min="0" max="1500000" liveChange="handleSynchronizeMinFocus" class="sapUiTinyMarginBottom" />

      <core:Title text="Preferences" />
      <Label text="Nightshift" />
      <Switch class="sapUiSmallMarginBottom" tooltip="Switch between normal and dark theme" change="handleToggleUserTheme" state="{=${Config>/settings/appearance/theme} === 'dark' ? true : false}" />
      <Label text="Session log" />
      <Switch class="sapUiSmallMarginBottom" tooltip="If activated, your pomodoro focus will be logged during this session" state="{Config>/settings/history/session}" />
      <Label text="Record history" />
      <Switch class="sapUiSmallMarginBottom" tooltip="If activated, your pomodoro focus will be logged in your browser. These data can be deleted by clicking on 'Delete history data' in the 'History' section" enabled="{Config>/settings/history/session}" state="{Config>/settings/history/persistent}" />
      <Label text="Desktop notifications" />
      <Switch class="sapUiSmallMarginBottom" state="{Config>/settings/notification/desktopNotification}" />

      <core:Title text="History and calender" />

      <Label text="Calender from" />
      <TimePicker width="4rem" value="{Config>/settings/history/calenderStartHour}" valueFormat="HH" displayFormat="HH"/>

      <Label text="Calender to" />
      <TimePicker width="4rem" value="{Config>/settings/history/calenderEndHour}" valueFormat="HH" displayFormat="HH"/>

      <Label text="Export history data" />
      <Button width="2rem" icon="sap-icon://download" tooltip="Export history data as .csv" press="handleExportHistory" />

      <Label text="Delete history data" />
      <Button width="2rem" type="Reject" icon="sap-icon://delete" tooltip="Delete history data stored on your computer" press="handleOpenHistoryDialog" />

    </f:content>
  </f:SimpleForm>
</mvc:View>