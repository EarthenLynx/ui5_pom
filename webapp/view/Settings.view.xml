<mvc:View controllerName="sap.ui.demo.basicTemplate.controller.Home" displayBlock="true"
  xmlns="sap.m"
  xmlns:l="sap.ui.layout"
  xmlns:f="sap.ui.layout.form"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns:core="sap.ui.core">
  <f:SimpleForm title="Settings" editable="true" layout="ResponsiveGridLayout" labelSpanXL="4" labelSpanL="4" labelSpanM="4" labelSpanS="12" adjustLabelSpan="false" emptySpanXL="0" emptySpanL="0" emptySpanM="0" emptySpanS="0" columnsXL="3" columnsL="2" columnsM="2" singleContainerFullSize="false">
    <f:toolbar>
      <Toolbar id="TB1">
        <Title id="Title1" text="Address"/>
        <ToolbarSpacer />
        <Button icon="sap-icon://reset" tooltip="Restore standard settings" press="handleResetUserSettings"/>
        <Button icon="sap-icon://save" tooltip="Save your settings" press="handleSetUserSettings" />
      </Toolbar>
    </f:toolbar>
    <f:content>
      <core:Title text="Pomodoro intervals" />
      <Label text="Focus interval - {path: 'Pomodoro>/settings/pomodoro/msTotal', formatter: '.formatter.formatMsToMinutes'}" />
      <Slider step="60000" value="{Pomodoro>/settings/pomodoro/msTotal}" min="0" max="3000000" liveChange="handleSynchronizeMinFocus" class="sapUiTinyMarginBottom" />

      <Label text="Short break - {path: 'Pomodoro>/settings/shortBreak/msTotal', formatter: '.formatter.formatMsToMinutes'}" />
      <Slider step="7500" value="{Pomodoro>/settings/shortBreak/msTotal}" min="0" max="600000" class="sapUiTinyMarginBottom" />

      <Label text="Long break - {path: 'Pomodoro>/settings/longBreak/msTotal', formatter: '.formatter.formatMsToMinutes'}" />
      <Slider step="30000" value="{Pomodoro>/settings/longBreak/msTotal}" min="0" max="1500000" class="sapUiTinyMarginBottom" />

      <Label text="Min. focus - {path: 'Pomodoro>/settings/minFocus/msTotal', formatter: '.formatter.formatMsToMinutes'}" />
      <Slider step="30000" value="{Pomodoro>/settings/minFocus/msTotal}" min="0" max="1500000" liveChange="handleSynchronizeMinFocus" class="sapUiTinyMarginBottom" />

      <core:Title text="Preferences" />
      <Label text="Nightshift" />
      <Switch class="sapUiSmallMarginBottom" tooltip="Switch between normal and dark theme" change="handleToggleUserTheme" state="{=${Pomodoro>/settings/appearance/theme} === 'dark' ? true : false}" />
      <Label text="Session log" />
      <Switch class="sapUiSmallMarginBottom" tooltip="If activated, your pomodoro focus will be logged during this session" state="{Pomodoro>/settings/history/session}" />
      <Label text="Record history" />
      <Switch class="sapUiSmallMarginBottom" tooltip="If activated, your pomodoro focus will be logged in your browser. These data can be deleted by clicking on 'Delete history data' in the 'History' section" enabled="{Pomodoro>/settings/history/session}" state="{Pomodoro>/settings/history/persistent}" />
      <Label text="Desktop notifications" />
      <Switch class="sapUiSmallMarginBottom" state="{Pomodoro>/settings/notification/desktopNotification}" />

      <core:Title text="History" />
      <Label text="Export history data" />
      <Button width="2rem" icon="sap-icon://download" tooltip="Export history data as .csv" press="handleExportHistory" />

      <Label text="Delete history data" />
      <Button width="2rem" type="Reject" icon="sap-icon://delete" tooltip="Delete history data stored on your computer" press="handleOpenHistoryDialog" />

    </f:content>
  </f:SimpleForm>
</mvc:View>